# 数据库配置
# PostgreSQL 数据库连接字符串
# 格式: postgresql://用户名:密码@主机:端口/数据库名?schema=public
DATABASE_URL="postgresql://user:password@localhost:5432/socialwiz?schema=public"

# Redis 配置（可选，默认使用本地 Redis）
REDIS_URL="redis://localhost:6379"

# JWT 密钥（请在生产环境中使用强随机字符串）
JWT_SECRET="your-secret-key-change-in-production"

# 加密密钥（用于加密AppSecret等敏感信息）
# 生成方式: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY="your-32-character-hex-encryption-key-here"

# 应用基础URL
NEXT_PUBLIC_BASE_URL="http://localhost:3000"

# AI Models API Keys
DEEPSEEK_API_KEY="your-deepseek-api-key"
QWEN_API_KEY="your-qwen-api-key"
OPENAI_API_KEY="your-openai-api-key"

# ============================================
# 平台接入说明（重要）
# ============================================
# 注意：社交媒体平台（微信、微博、抖音、小红书等）的 APP_ID 和 APP_SECRET
# 不再在此配置文件中设置。这些凭证应该与用户维度绑定，存储在数据库中。
#
# 原因：
# 1. 每个用户需要配置自己的平台应用凭证
# 2. 支持多账号：同一用户可以连接多个平台账号
# 3. 数据隔离：确保用户之间的平台凭证完全独立
# 4. 安全性：APP_SECRET 在数据库中加密存储，与用户关联
#
# 配置方式：
# - 微博：用户在应用中配置 APP_ID 和 APP_SECRET，通过 OAuth 授权连接
# - 微信公众号：用户手动输入 AppID 和 Secret（个人开发者限制）
# - 其他平台：根据平台特点采用 OAuth 或手动配置方式
#
# 如果您是从旧版本升级，请参考 specs/003-weibo-integration 和
# specs/005-wechat-integration 中的数据迁移说明。
# ============================================

# 微信网页授权配置（用于OAuth测试）
# 重要：
# 1. redirect_uri 必须与微信公众平台配置的“网页授权域名”一致
# 2. 不支持 localhost，本地开发需使用内网穿透工具：
#    - ngrok: https://ngrok.com/
#    - localtunnel: https://localtunnel.github.io/www/
# 3. 建议使用 https 协议确保安全性
# 示例配置：
# WECHAT_APP_ID=wx1234567890abcdef
# WECHAT_APP_SECRET=your_secret_here
# WECHAT_REDIRECT_URI=https://abc123.ngrok.io/api/platforms/wechat/auth/callback
WECHAT_APP_ID=
WECHAT_APP_SECRET=
WECHAT_REDIRECT_URI=

# Object Storage (OSS)
OSS_ACCESS_KEY="your-oss-access-key"
OSS_SECRET_KEY="your-oss-secret-key"
OSS_BUCKET="your-bucket-name"
OSS_REGION="your-region"
OSS_ENDPOINT="https://oss-cn-hangzhou.aliyuncs.com"

# OpenAI API 配置
# 从 https://platform.openai.com/api-keys 获取您的 API 密钥
OPENAI_API_KEY=your_openai_api_key_here

# 可选：其他环境变量
# NODE_ENV=development

# DeepSeek API 配置（如果使用 DeepSeek 模型）
# 从 https://platform.deepseek.com/api_keys 获取您的 API 密钥
DEEPSEEK_API_KEY=your_deepseek_api_key_here
